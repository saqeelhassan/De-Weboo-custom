# Block direct HTTP access to layout fragments (they are meant for include/require only).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Deny from all
</IfModule>
